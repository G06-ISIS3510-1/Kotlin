{
  "name": "wheels-functions",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf lib",
    "lint": "eslint --ext .ts src",
    "serve": "firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions",
    "test": "npm run build && node --test lib/**/*.test.js",
    "seed:cancellations": "node scripts/seed_cancellations.js",
    "clear:cancellations": "node scripts/clear_cancellations.js",
    "seed:create-ride-location-usage": "node scripts/seed_create_ride_location_usage.js"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.7.3"
  }
}
